function happy
{
    if(b() == 51)
    {
        while(a() < b())
        {
            c = c + 1;
        }
        c = c * sqrt(a() - b());
    }
    else
    {
        while(a() > b())
        {
            c = c - 1;
        }
        c = c * sqrt(a() + b());
    }
}

